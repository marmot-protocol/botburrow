#!/usr/bin/env ruby
require_relative "../config/environment"

listener = BotListener.new

trap("INT")  { listener.shutdown }
trap("TERM") { listener.shutdown }

begin
  listener.run
rescue BotListener::AlreadyRunningError => e
  abort e.message
end
