<h1 class="mb-6 text-xl font-semibold">New scheduled action for <%= @bot.name %></h1>

<div class="max-w-6xl rounded-lg border border-border bg-surface-raised p-6">
  <%= render "form", bot: @bot, scheduled_action: @scheduled_action %>
</div>
