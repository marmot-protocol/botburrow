<%= back_link @bot.name, bot_path(@bot, anchor: "schedules") %>
<h1 class="mb-6 text-xl font-semibold">Edit scheduled action</h1>

<div class="max-w-6xl rounded-lg border border-border bg-surface-raised p-6">
  <%= render "form", bot: @bot, scheduled_action: @scheduled_action, groups: @groups %>
</div>
