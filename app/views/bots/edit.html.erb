<%= back_link @bot.name, bot_path(@bot) %>
<h1 class="mb-6 text-xl font-semibold">Edit bot</h1>

<div class="max-w-lg rounded-lg border border-border bg-surface-raised p-6">
  <%= render "form", bot: @bot %>
</div>
