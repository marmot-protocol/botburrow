<%= back_link @bot.name, bot_path(@bot, anchor: "commands") %>
<h1 class="mb-6 text-xl font-semibold">New command</h1>

<div class="max-w-6xl rounded-lg border border-border bg-surface-raised p-6">
  <%= render "form", bot: @bot, command: @command %>
</div>
