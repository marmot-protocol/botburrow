<h1 class="mb-6 text-xl font-semibold">New command for <%= @bot.name %></h1>

<div class="max-w-lg rounded-lg border border-border bg-surface-raised p-6">
  <%= render "form", bot: @bot, command: @command %>
</div>
